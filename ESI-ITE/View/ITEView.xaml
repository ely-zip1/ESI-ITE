<UserControl x:Class="ESI_ITE.View.ITEView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ESI_ITE.View"
      xmlns:vm="clr-namespace:ESI_ITE.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="700">
    <UserControl.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="White" Offset="0"/>
            <GradientStop Color="#FF91DCFF" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Background>

    <UserControl.DataContext>
        <vm:ITEViewModel x:Name="viewModel"></vm:ITEViewModel>
    </UserControl.DataContext>

    <Frame Name="frameITE" Content="{Binding SelectedPage}" Padding="20 " NavigationUIVisibility="Hidden" Margin="5"
           HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
</UserControl>
