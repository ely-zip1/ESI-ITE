<Window x:Class="ESI_ITE.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ESI_ITE.ViewModel"
        xmlns:myControls="clr-namespace:ESI_ITE.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="768" Width="1024">
    <Window.Resources>
        <local:TransactionEntryViewModel x:Key="transEntryVM"/>
    </Window.Resources>
    <Grid HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="341"></ColumnDefinition>
            <ColumnDefinition Width="682"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>


        <!--Region Transaction Entry-->
        <myControls:TransactionEntryView x:Name="ucTransactionEntry" Grid.Column="0" Grid.Row="1" Width="auto"/>
        <!--End Region-->
    </Grid>
</Window>
