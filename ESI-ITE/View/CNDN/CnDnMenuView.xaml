<Page x:Class="ESI_ITE.View.CNDN.CnDnMenuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ESI_ITE.View.CNDN"
      xmlns:vm="clr-namespace:ESI_ITE.ViewModel.CNDN"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="CnDnMenuView">

    <Page.DataContext>
        <vm:CnDnMenuViewModel x:Name="viewModel"/>
    </Page.DataContext>

    <Grid>
        <StackPanel Orientation="Vertical">
            <Button Content="CN\DN Entry" Margin="10" Command="{Binding EntryCommand}"/>
            <Button Content="CN\DN Printing" Margin="10" Command="{Binding PrintCommand}"/>
            <Button Content="CN\DN Posting" Margin="10" Command="{Binding PostCommand}"/>
        </StackPanel>
    </Grid>
</Page>
